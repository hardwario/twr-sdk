# Repository "bc-core-module-sdk"

This repository contains complete SDK for Core Module (libraries, headers, documentation, etc.).

It is used as a Git submodule of firmware repository (e.g. bc-core-module).
